# 安全说明

本项目是可自行部署的电商内容工具。管理员负责保存自己的模型凭证、业务素材、账户数据和备份。生产环境请使用 HTTPS，配置独立随机密钥，并限制 `data/` 与 `.env.local` 文件权限。

请勿在公开 Issue 中提交密钥、可用登录令牌、私有素材、个人联系方式或可直接攻击现有部署的详细步骤。一般缺陷可用虚构数据最小化复现后提交；敏感漏洞请先使用仓库 Security 页提供的私密报告入口（若已启用），否则先发不含细节的报告请求，等待维护者提供私密渠道。

发布检查包含依赖审计、密钥扫描、权限和输入处理测试，不能代替持续维护或第三方安全审计。未经模型服务配置和真实调用验证的功能，不应被视为供应商连通性保证。
